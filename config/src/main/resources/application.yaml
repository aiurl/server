server:
    port: 8900

spring:
    application:
        name: config
    profiles:
        active: ${SPRING_PROFILES_ACTIVE:mongodb}
    security:
        user:
            name: ${CONFIG_SERVER_USERNAME:config-user}
            password: ${CONFIG_SERVER_PASSWORD:config-password}
    mongodb:
        uri: ${MONGO_URI:mongodb://host.docker.internal:27017/linkyou?authSource=admin}
    data:
        redis:
            url: ${REDIS_URL:redis://localhost:6379}
    cloud:
        config:
            server:
                native:
                    search-locations: ${CONFIG_SEARCH_LOCATIONS:file:./config}
                mongodb:
                    collection: ${CONFIG_MONGO_COLLECTION:properties}
                    enabled: true
                    order: 1
                    default-label: ${CONFIG_MONGO_DEFAULT_LABEL:master}
